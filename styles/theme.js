import { extendTheme, theme as base } from "@chakra-ui/react";
const theme = extendTheme({
    fonts: {
        heading: `RanuaTrials`,
        body: `RanuaTrials`,
      },
      colors: {
        orange: {
          300: '#F67B08'
        }
      },
});

export default theme;


